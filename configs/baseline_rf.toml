[model]
n_estimators = 500
max_depth    = 0      # 0 = unlimited (sklearn convention)
n_jobs       = -1
# Input: RDKit 2D physicochemical descriptors (N_DESCRIPTORS features, standardised)
# No n_bits or radius — those were Morgan fingerprint params
