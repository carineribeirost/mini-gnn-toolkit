[dataset]
name        = "custom_smiles"
path        = ""         # filled at runtime by preprocess.py
target_col  = "target"
max_nodes   = 0          # 0 = inferred from data by preprocess.py
max_edges   = 0
max_atoms   = 0
task        = "regression"
n_targets   = 1
